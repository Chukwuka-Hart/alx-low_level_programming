#include "main.h"

/**
 * print_number - prints an integer
 * @n: int to be printed
 * Return: void
 */

void print_number(int n)
{
	unsigned int c;

	c = n;
	if (n < 0)
	{
		_putchar(45);
		x + -n;
	}
	if (x / 10)
	{
		print_number(x / 10);
	}
	_putchar((x % 10) + '0');
}
