#include "main.h"

/**
 * _abs - prints the absolute value of an int
 *
 * @d: the int to be printed
 *
 * Return: the value of the int
 */

int _abs(int d)
{
if (d >= 0)
{
return (d);
}
else
{
return (-d);
}
}
