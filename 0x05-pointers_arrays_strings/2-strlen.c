#include "main.h"

/**
 * _strlen - returns the lenth if a string
 *
 * @s: string
 *
 * Return: strlen
 */

int _strlen(char *s)
{
	int length;

	s = " ";
	length = _strlen(s);


	return (length);
}
