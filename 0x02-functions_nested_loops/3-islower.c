#include "main.h"

/**
 * _islower - checks for lowercase characters
 *
 *c: char to be checked
 *
 * Return: 1 if lowercase, 0 if otherwise
 */

int _islower(int c)
{
  if (_islower(c) <= 'z')
{
return (1);
}
else
{
return (0);
}
}
